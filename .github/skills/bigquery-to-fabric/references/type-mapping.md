# Type mapping

Direct mappings include BOOL, INT64, FLOAT64, NUMERIC, STRING, BYTES, and DATE.

Review BIGNUMERIC precision, TIMESTAMP timezone semantics, JSON projection, GEOGRAPHY
operations, and every ARRAY/STRUCT flattening decision. Preserve raw nested data in a
Bronze Lakehouse when lossless relational normalization is not proven.
